<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Camera permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Gallery / storage permissions (for photo picking) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Microphone — voice messages and voice/video calls -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Notifications — Android 13+ runtime permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <!-- Phone state -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Storage -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" tools:replace="maxSdkVersion" />

    <!-- ═══ NOTIFICATION SYSTEM PERMISSIONS (WhatsApp-grade) ═══ -->
    <!-- Foreground services for push processing -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Wake lock for push processing -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Exact alarm scheduling -->

    <!-- Vibration -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Receive boot completed (re-register tokens, reschedule alarms) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Full screen intent for incoming calls -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:label="Kora Messenger"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:theme="@style/Theme.KoraMessenger">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/Theme.KoraMessenger"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/Theme.KoraMessenger" />
        </activity>

        <!-- ── App icon activity aliases ── -->
        <activity-alias
            android:name=".IconClassic"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_classic"
            android:label="Kora Messenger"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".IconAuroraCircle"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_aurora_circle"
            android:label="Kora Messenger"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".IconGoldElite"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_gold_elite"
            android:label="Kora Messenger"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ═══ NOTIFICATION SYSTEM COMPONENTS ═══ -->

        <!-- FCM Service — receives push notifications -->
        <service
            android:name=".notifications.KoraFcmService"
            android:exported="false"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Foreground Service (processes push, prevents killing) -->
        <service
            android:name=".notifications.KoraForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Live Call Translation Service — runs during active calls
             with microphone + phone call foreground service types.
             Android 14+ (API 34) requires explicit type declaration.
             Lifecycle is bound to KoraCallNotification (visible notification)
             so the OS does not terminate translation mid-call. -->
        <service
            android:name=".LiveCallTranslationService"
            android:exported="false"
            android:foregroundServiceType="microphone|phoneCall"
            android:stopWithTask="false" />

        <!-- Direct Reply Receiver (quick reply from notification) -->
        <receiver
            android:name=".notifications.KoraDirectReplyReceiver"
            android:exported="false" />

        <!-- Call Action Receiver (accept/reject/hangup) -->
        <receiver
            android:name=".notifications.KoraCallActionReceiver"
            android:exported="false" />

        <!-- Notification Dismissed Receiver (per-type dismiss tracking) -->
        <receiver
            android:name=".notifications.KoraNotificationDismissedReceiver"
            android:exported="false" />

        <!-- Boot Receiver (re-register tokens, reschedule alarms) -->
        <receiver
            android:name=".notifications.KoraBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Alarm Receiver (periodic scheduled tasks) -->
        <receiver
            android:name=".notifications.KoraAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kora.messenger.action.DAILY_CRON" />
                <action android:name="com.kora.messenger.action.HOURLY_CRON" />
                <action android:name="com.kora.messenger.action.HEARTBEAT_WAKEUP" />
                <action android:name="com.kora.messenger.action.ROTATE_SIGNED_PREKEY" />
                <action android:name="com.kora.messenger.action.BACKUP_MESSAGES" />
            </intent-filter>
        </receiver>

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>
</manifest>
